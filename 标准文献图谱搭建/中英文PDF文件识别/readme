在上述文件之外的，重新对标准PDF文件的识别方法做了测试。
PDF文件可以分为中英文两种，按照生成格式分为扫描件和转换件（直接由word转换而来）
在转换件中，直接使用PDFParser完成了PDF2text的转换。
在扫描件中，尝试了将PDF分为多张图片，然后，使用OCR技术识别的方式。（利用的工具有：CnOCR/easyocr）
但是，发现，在图片识别中，网址elfinbook的识别效果较佳，因此，直接采用API调用的方式，在线识别。
将识别的图片内容拼接为text文件。



在第二天的尝试中，发现，全能扫描王的端口，可以完成PDF2word的转换，且转换准确率高，格式保持正确。
因此，最终，拟采用全能扫描王提供的端口完成PDF2word的转换。
之后，也进行了word2text的转化。



（全能扫描王端口可以会封，所以，传统工具包也是有用的）
完整资源地址：https://download.csdn.net/download/Hekena/86340700
